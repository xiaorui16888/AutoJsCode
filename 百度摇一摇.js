var oldTime = new Date(); 
while (true) { 
    var newTime = new Date(); 
    if (newTime - oldTime > 27*60*1000) { exit(); } 
    else {  
            click(542,1686);
            
            } 
              }